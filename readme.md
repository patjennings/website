Personal website. Design. Motion. [live @ thomas.guesnon.fr](https://www.thomasguesnon.fr)

```bash
gem install bundler # install bundler
bundle install # install jekyll and dependencies
```

```bash
bundle exec jekyll serve # launch jekyll
```